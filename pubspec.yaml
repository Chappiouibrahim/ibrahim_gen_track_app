name: gen_track_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0' # Assure-toi que ta version de Dart est compatible

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # --- Firebase Dependencies ---
  firebase_core: ^2.32.0  # Ou la dernière version stable
  firebase_auth: ^4.18.0  # Ou la dernière version stable
  # ----------------------------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
